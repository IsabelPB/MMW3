import HeaderMain from "@/components/HeaderMain";

export default function Profile(){
    return(
        <main>
            <div>
                <HeaderMain />s
            </div>
        </main>
    )
}